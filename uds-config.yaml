options:
  tmp_dir: /var/tmp
shared:
  domain: uds.local
variables:
  postgres-operator:
    POSTGRESQL:
      enabled: true
      teamId: "uds"
      volume:
        size: "30Gi"
      numberOfInstances: 1
      users:
        keycloak.keycloak: []  # database owner
        mattermost.mattermost: []
        grafana.grafana: []
        gitea.gitea: []
      databases:
        keyblockdb: keycloak.keycloak
        mattermostdb: mattermost.mattermost
        grafanadb: grafana.grafana
        giteadb: gitea.gitea
      version: "14"
      ingress:
        - remoteNamespace: keycloak
        - remoteNamespace: mattermost
        - remoteNamespace: grafana
        - remoteNamespace: gitea